# Base de datos
DB_HOST=localhost
DB_PORT=3306
DB_USER=freshcut
DB_PASSWORD=freshcut_secret
DB_NAME=freshcut

# JWT
JWT_SECRET=cambia-esto-en-produccion
JWT_REFRESH_SECRET=cambia-esto-tambien
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Servidor
PORT=3000
NODE_ENV=development

# Cancelación: horas mínimas de anticipación para cancelar sin penalización
CANCEL_HOURS_THRESHOLD=4

# Admin seed (se crea al correr el seed)
ADMIN_EMAIL=admin@freshcut.com
ADMIN_PASSWORD=admin123
  